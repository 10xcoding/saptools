namespace SAPTOOLS.BACKEND.data;   // define the package under which this file is created

@Schema: 'SAPTOOLS'    // defines the schema under which the created table(s) would reside under

context table {     // same as the file name before .hdbdd
 	Entity ttable {     // declares a table (entity)
        key TCode: String(100) not null;
        Description: String(1000);
        Program: String(1000);
    };
};